import { View, Text } from "react-native";

export default function DocumentScreen() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Documents</Text>
    </View>
  );
}